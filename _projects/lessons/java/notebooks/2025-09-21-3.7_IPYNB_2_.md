---
layout: post
courses: {'csa': {'week': 3}}
categories: ['Java', 'Class-Variables-and-Methods']
lesson_language: Java
lesson_topic: Class-Variables-and-Methods
lesson_part: interactive
lesson_type: lesson
toc: True
codemirror: True
title: Class Variables and Methods
menu: nav/csa_units/csaunit3.html
permalink: /csa/unit_03/3_7
---

## Lesson Objectives
- Define **class variables** and **class methods** using `static`
- Explain how class variables are shared by all objects of a class, and what class methods can/cannot access
- Use a **class name and dot operator** to access public class variables/methods
- Explain what the `final` keyword does

## Instance vs. Class Variables

Recall that an *instance variable belongs to an individual object*, so let's say we have:
```java
public class Student {
    private String name;
    private int grade;
}
```
and we define 3 students
```java
Student s1 = new Student();
Student s2 = new Student();
Student s3 = new Student();
```

Each student gets their own `name` and `grade`.

Class variables are different in that they *belong to the class itself*:
```java
public class Student {
    private String name;
    private int grade;

    public static int numberOfStudents = 0;
}
```

Now, each student `s1`, `s2`, `s3`, shares one copy of `numberOfStudents`.

For example, we could make a class:
```java
public class Student {
    private String name;

    public static int numberOfStudents = 0;

    public Student(String n) {
        name = n;
        numberOfStudents++;
    }
}
```
and create a few objects:
```java
Student a = new Student("Alice");
Student b = new Student("Bob");
Student c = new Student("Charlie");
```
which makes `Student.numberOfStudents` `3`.

## Static

We use `static` to denote that a variable or method belongs to the class itself rather than to an individual object.

This would be an instance variable:
```java
private String name;
```
and this would be a class variable:
```java
private static int numberOfStudents;
```

## Public and Private

If we want some class variable to be accessible outside of the class, we can declare it as `public`:
```java
public class Game {
    public static int players = 0;
}
```
which means we can do:
```java
System.out.println(Game.players); // get # of players
Game.players = 5; // modify # of players
```


## Class Methods

Similarly, we can define a method as `static` to denote it belongs to a class:
```java
public class MathHelper {

    public static int doubleValue(int x) {
        return x * 2;
    }
}
```

Now we can call it without creating a `MathHelper` object:
```java
int result = MathHelper.doubleValue(5);

System.out.println(result); // 10
```

Class methods can access class variables, other class methods, its parameters, and local variables, but NOT instance variables:
```java
public class Counter {

    private static int count = 0;

    public static void increase() {
        count++;
    }

    public static int getCount() {
        return count;
    }

    public static void reset() {
        count = 0;
    }

    public static void resetAndIncrease() {
        reset();
        increase();
    }
}
```

We CANNOT do this
```java
public class Student {
    private String name;

    public static void printName() {
        System.out.println(name); // ERROR
    }
}
```

## `final` Variables

We define a variable as `final` if we want it to be immutable.
```java
public static final int DAYS_IN_WEEK = 7;
```

## Homework

In this assignment, you'll be modifying a partiall implemented Java class called `GameTracker`. The bottom half of the code snippet runs debugging tests as you progress with the TODOs.

{% capture game_tracker %}
public class GameTracker {

    // =========================================================
    // CONSTANTS
    // =========================================================

    public static final int MAX_SCORE = 1000;


    // =========================================================
    // CLASS VARIABLES
    // =========================================================

    // TODO 1:
    // Declare a private static variable that keeps track
    // of how many GameTracker objects have been created.



    // TODO 2:
    // Declare a private static variable that keeps track
    // of the total number of points scored by ALL players.



    // =========================================================
    // INSTANCE VARIABLES
    // =========================================================

    private String playerName;
    private int score;


    // =========================================================
    // CONSTRUCTOR
    // =========================================================

    public GameTracker(String name) {
        playerName = name;
        score = 0;

        // TODO 3:
        // Update the appropriate class variable.
    }


    // =========================================================
    // INSTANCE METHODS
    // =========================================================

    public void addPoints(int points) {

        // TODO 4:
        // Add the points to this player's score.
        //
        // Also update the appropriate class variable.
    }


    public int getScore() {

        // TODO 5:
        // Return this player's score.

        return 0;
    }


    public String getPlayerName() {

        // TODO 6:
        // Return this player's name.

        return "";
    }


    public void resetScore() {

        // TODO 7:
        // Reset this player's score.
        //
        // Remember that the class variable tracking
        // total points must also be updated.
    }


    // =========================================================
    // CLASS METHODS
    // =========================================================

    public static int getPlayerCount() {

        // TODO 8:
        // Return the appropriate class variable.

        return 0;
    }


    public static int getTotalPoints() {

        // TODO 9:
        // Return the appropriate class variable.

        return 0;
    }


    public static void printPlayer(GameTracker player) {

        // TODO 10:
        // Use the parameter to print the player's name
        // and score.
    }


    public static double getPercentOfMax(GameTracker player) {

        // TODO 11:
        // Calculate and return the player's score
        // as a percentage of MAX_SCORE.

        return 0.0;
    }


    public static boolean hasReachedMax(GameTracker player) {

        // TODO 12:
        // Return true if this player's score is
        // greater than or equal to MAX_SCORE.

        return false;
    }


    // =========================================================
    // TESTS
    // =========================================================

    public static void main(String[] args) {

        System.out.println("=================================");
        System.out.println("      GAMETRACKER TESTS");
        System.out.println("=================================");
        System.out.println();

        testPlayerCount();
        testInitialScore();
        testPlayerNames();
        testAddPoints();
        testTotalPoints();
        testMultiplePlayers();
        testResetScore();
        testPrintPlayer();
        testPercentOfMax();
        testHasReachedMax();
        testMaxScoreConstant();

        System.out.println();
        System.out.println("=================================");
        System.out.println("      ALL TESTS COMPLETE");
        System.out.println("=================================");
    }


    // ---------------------------------------------------------
    // TEST 1
    // ---------------------------------------------------------

    public static void testPlayerCount() {

        System.out.println("Test 1: Player Count");

        GameTracker a = new GameTracker("Alex");
        GameTracker b = new GameTracker("Maria");
        GameTracker c = new GameTracker("Jordan");

        if (GameTracker.getPlayerCount() == 3) {
            System.out.println("[ PASS ] Player count");
        } else {
            System.out.println("[ FAIL ] Player count");
            System.out.println("         Expected: 3");
            System.out.println(
                "         Actual: " + GameTracker.getPlayerCount()
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 2
    // ---------------------------------------------------------

    public static void testInitialScore() {

        System.out.println("Test 2: Initial Score");

        GameTracker player = new GameTracker("Sam");

        if (player.getScore() == 0) {
            System.out.println("[ PASS ] Initial score is 0");
        } else {
            System.out.println("[ FAIL ] Initial score");
            System.out.println("         Expected: 0");
            System.out.println(
                "         Actual: " + player.getScore()
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 3
    // ---------------------------------------------------------

    public static void testPlayerNames() {

        System.out.println("Test 3: Player Name");

        GameTracker player = new GameTracker("Taylor");

        if (player.getPlayerName().equals("Taylor")) {
            System.out.println("[ PASS ] Player name");
        } else {
            System.out.println("[ FAIL ] Player name");
            System.out.println("         Expected: Taylor");
            System.out.println(
                "         Actual: " + player.getPlayerName()
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 4
    // ---------------------------------------------------------

    public static void testAddPoints() {

        System.out.println("Test 4: Add Points");

        GameTracker player = new GameTracker("Chris");

        player.addPoints(25);
        player.addPoints(30);

        if (player.getScore() == 55) {
            System.out.println("[ PASS ] Add points");
        } else {
            System.out.println("[ FAIL ] Add points");
            System.out.println("         Expected: 55");
            System.out.println(
                "         Actual: " + player.getScore()
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 5
    // ---------------------------------------------------------

    public static void testTotalPoints() {

        System.out.println("Test 5: Total Points");

        GameTracker a = new GameTracker("A");
        GameTracker b = new GameTracker("B");

        a.addPoints(20);
        b.addPoints(30);

        if (GameTracker.getTotalPoints() == 50) {
            System.out.println("[ PASS ] Total points");
        } else {
            System.out.println("[ FAIL ] Total points");
            System.out.println("         Expected: 50");
            System.out.println(
                "         Actual: " + GameTracker.getTotalPoints()
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 6
    // ---------------------------------------------------------

    public static void testMultiplePlayers() {

        System.out.println("Test 6: Independent Player Scores");

        GameTracker a = new GameTracker("Alex");
        GameTracker b = new GameTracker("Maria");

        a.addPoints(100);
        b.addPoints(200);

        if (a.getScore() == 100 &&
            b.getScore() == 200) {

            System.out.println("[ PASS ] Independent scores");

        } else {

            System.out.println("[ FAIL ] Independent scores");
            System.out.println(
                "         Alex expected: 100, actual: "
                + a.getScore()
            );
            System.out.println(
                "         Maria expected: 200, actual: "
                + b.getScore()
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 7
    // ---------------------------------------------------------

    public static void testResetScore() {

        System.out.println("Test 7: Reset Score");

        GameTracker player = new GameTracker("Alex");

        player.addPoints(100);
        player.resetScore();

        if (player.getScore() == 0) {
            System.out.println("[ PASS ] Reset score");
        } else {
            System.out.println("[ FAIL ] Reset score");
            System.out.println("         Expected: 0");
            System.out.println(
                "         Actual: " + player.getScore()
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 8
    // ---------------------------------------------------------

    public static void testPrintPlayer() {

        System.out.println("Test 8: Print Player");

        GameTracker player = new GameTracker("Morgan");

        player.addPoints(75);

        System.out.println("Expected:");
        System.out.println("Player: Morgan");
        System.out.println("Score: 75");

        System.out.println();
        System.out.println("Your output:");
        GameTracker.printPlayer(player);

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 9
    // ---------------------------------------------------------

    public static void testPercentOfMax() {

        System.out.println("Test 9: Percent of Maximum");

        GameTracker player = new GameTracker("Alex");

        player.addPoints(250);

        double result = GameTracker.getPercentOfMax(player);

        if (result == 25.0) {
            System.out.println("[ PASS ] Percent of max");
        } else {
            System.out.println("[ FAIL ] Percent of max");
            System.out.println("         Expected: 25.0");
            System.out.println(
                "         Actual: " + result
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 10
    // ---------------------------------------------------------

    public static void testHasReachedMax() {

        System.out.println("Test 10: Has Reached Maximum");

        GameTracker player = new GameTracker("Jordan");

        player.addPoints(GameTracker.MAX_SCORE);

        if (GameTracker.hasReachedMax(player)) {
            System.out.println("[ PASS ] Has reached max");
        } else {
            System.out.println("[ FAIL ] Has reached max");
            System.out.println(
                "         Expected: true"
            );
            System.out.println(
                "         Actual: false"
            );
        }

        System.out.println();
    }


    // ---------------------------------------------------------
    // TEST 11
    // ---------------------------------------------------------

    public static void testMaxScoreConstant() {

        System.out.println("Test 11: MAX_SCORE Constant");

        if (GameTracker.MAX_SCORE == 1000) {
            System.out.println("[ PASS ] MAX_SCORE constant");
        } else {
            System.out.println("[ FAIL ] MAX_SCORE constant");
            System.out.println("         Expected: 1000");
            System.out.println(
                "         Actual: " + GameTracker.MAX_SCORE
            );
        }

        System.out.println();
    }
}
{% endcapture %}
{% include runners/code.html runner_id="java-main" language="java" code=game_tracker %}
